<h1>Products#Index</h1>

<% cart.each do |item| %>
  <ul>
    <li><%= item %></li>
  </ul>
<% end %>

<%= form_tag('products/add') do %>
  <%= label_tag :product %>
  <%= text_field_tag :product %>
  <%= button_tag 'add to cart' %>
<% end %>