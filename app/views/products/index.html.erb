<h1>Products#index</h1>
    <ul>
    <% cart.each do |product| %>
      <li><%= product %></li>
    <% end %>
  </ul>
<%= form_tag({controller: "products", action: "add"}, method: "get") %>

<p><%= text_field_tag 'product' %></p>
<p><%= submit_tag 'add to cart' %></p>
