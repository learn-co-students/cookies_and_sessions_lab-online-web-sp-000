<ul>
    <% cart.each do |item| %>
        <li><%= item %>
    <% end %>
</ul>

<%= form_tag new_product_path do |f| %>
    <%= label_tag :product %>
    <%= text_field_tag :product %>
    <%= submit_tag 'add to cart' %>
<% end %>