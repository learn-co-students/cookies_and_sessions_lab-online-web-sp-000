<h1>Session Cart</h1>
<%= form_tag '/add', method: :post do %>
    <%= text_field_tag :product %>
    <%= submit_tag "add to cart" %>
<% end %>

<% if !cart.empty? %>
    <ul>
        <% cart.each do |product| %>
            <li><%= product %></li>
        <% end %>
    </ul>
<% end %>