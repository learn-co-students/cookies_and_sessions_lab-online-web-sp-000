<% if cart.empty? %>
  Cart is empty
<% else %>
  <% cart.each do |item| %>
    <%= item %>
  <% end %>
<% end %>

<%= form_tag ({controller: 'products',
    action: 'add',
    method: 'post'}) do %>

  <input name=product>
  <input type=submit value='add to cart'>
<% end %>
