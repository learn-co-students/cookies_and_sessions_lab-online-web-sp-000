
<label>Here are the items in your cart:<label>

    <ul>
    <% session[:cart].each do |i| %>
        <li><%= i %></li>
    <% end %>
    </ul>

<%= form_tag products_add_path do |f| %>
<label> Add a product <label>>
<%= text_field_tag :product%>
<%= submit_tag "add to cart" %>
<% end %>
