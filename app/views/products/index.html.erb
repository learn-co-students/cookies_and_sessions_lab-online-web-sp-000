<h1>Products in the cart:</h1>
<ul>
    <%= cart.each do |product| %>
        <li><%= product %></li>
    <% end %>
</ul>

<br>
<%= form_tag(products_add_path) do %>
    <%= label_tag :add_product %>
    <%= text_field_tag :product %>
    <%= submit_tag "add to cart" %>
<% end %>